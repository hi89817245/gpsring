# GPS 鴿環 Q&A 對談紀錄

## 2026-05-24：v0.2.0 真實防弊沙盤對齊

### Q1：為何前面回答突然消失？會不會影響工作？
A：目前看起來是 WebUI / 工具呼叫輪數 / context compaction（上下文壓縮）造成的顯示中斷，不代表 repo 檔案或 git commit 消失。已完成的實際成果仍以 git commit、檔案內容與服務驗證為準。為避免關鍵設計遺失，後續重要對齊內容會補進本檔。

### Q2：原本模擬資料為何不專業？
A：舊版資料比較像工程師隨機生點，沒有完整反映「鴿性、風向、人性作弊、GPS 故障」差異。使用者指出正常/高速/高鐵/AB 範本相似、點數太少、末端才紅、作弊劇本不合人性，判斷正確。

### Q3：v0.2.0 做了什麼？
A：已重寫 `train_dataset_generator.py` 與 `fraud_engine.py`：
- `normal`：正常鴿性，加入風偏、曲線、速度起伏。
- `cheat_highway`：基隆/大武崙接應 → 國道/快速道路南下 → 頭份交流道附近釋放。
- `cheat_hsr`：台北/板橋 → 苗栗高鐵站，單一路徑，不再做不合人性的多次交通工具切換。
- `suspicious_ab`：AB 舍/接應點低速滯留。
- `gps_fault`：HDOP 高、衛星數低、GPS 漂移，獨立標為設備/定位疑慮，不直接判作弊。

### Q4：規則碼如何對應？
A：
- GPS-R01：超物理速度。
- GPS-R02：貼地高速。
- GPS-R03：高速/快速道路路廊重合。
- GPS-R04：高鐵路廊重合。
- GPS-R05：非登記地點滯留 / AB 舍 / 接應點。
- GPS-R06：GPS 品質異常 / 設備定位疑慮。
- GPS-R07：低頻不可判定或證據不足。

### Q5：為何 v0.2.0 上傳後只看到少數幾點？
A：v0.2.0 預設低頻 300s 是正確方向，但前端抽稀演算法錯用 `idx % 30`。新版 CSV 是 60 秒一筆，`idx % 30` 等於每 1800 秒才顯示一點，導致畫面點數過少、甚至漏掉高速/高鐵警示。v0.2.1 修正為依照實際 `timestamp` 秒數抽稀：10s / 180s / 300s。

### Q6：為何上傳高速範本後沒有錯誤警示？
A：CSV 內的 `id` 可能是 `G0703-CHEATER`，但表單設備 ID 是 `G0703-TEST`。上傳後後端實際存到 CSV 內的 id，前端卻載入表單 device，造成載錯軌跡。v0.2.1 後端會回傳 `primary_track_id`，前端自動載入 CSV 內實際 ID，並在狀態列提示。

### Q7：為何拖曳點位會出現 `Cannot set properties of null (setting 'innerText')`？
A：JS 在拖曳後嘗試更新 `sandbox-badge`，但 HTML 沒有這個 DOM 元素。v0.2.1 已補上 `sandbox-badge`，並加上 null guard，避免未來正式模式或面板未顯示時再爆錯。

### Q8：mouseover 希望顯示哪些資訊？
A：v0.2.1 已把 Tooltip 擴充為：設備 ID、點號、台北時間、速度、高度、狀態與規則碼，方便逐點人工分析。

## 2026-05-24：v0.3.0 藍圖需求對齊

### Q9：為何高速/高鐵作弊不應只靠幾個點判定？
A：使用者指出正確：若只是少數點貼近高速/高鐵路線，並不足以構成交通工具作弊證據。真正合理的判定應要求「連續 N 公里貼近路廊」，預設 N=20km。否則可能只是路線交會、地圖誤差、低頻抽稀或 GPS 漂移。

### Q10：連續路廊判定應如何做？
A：應以 segment/run 為單位：逐段計算距高速/高鐵路廊距離、速度、高度與段長，將連續符合條件的段落合併。只有累積距離達到 `min_corridor_match_km=20` 時，才升級為高危作弊；不足者標為 partial/review。

### Q11：作弊點是否都在基隆或 A/B 舍？
A：不是。使用者補充不法擄鴿集團可能在山區架設攔鳥網。系統重點應是快速找出「疑似擄鴿/攔鳥網起點」與「轉運起點」，以便通知會長或相關人員處理。

### Q12：應新增哪些防弊事件與圖示？
A：建議新增 GPS-R08 疑似擄鴿/攔鳥網起點、GPS-R09 山區異常滯留、GPS-R10 轉運起點。地圖圖示建議：🕸️ 疑似攔鳥網、🚩 高危作弊起點、🏚️ A/B 舍或中繼舍、🚗 車輛轉運、🚄 高鐵轉運、🟣 GPS 定位異常。

### Q13：是否需要高度/時間維度視窗？
A：需要。單看平面地圖不夠，應新增 timeline 小視窗，X 軸為時間，Y 軸顯示高度/速度，並與地圖 hover/click 互動：滑過點位時 timeline 高亮，滑過 timeline 時地圖 marker 高亮。

### Q14：為何同三點或整條路都顯示 GPS-R01/GPS-R02/GPS-R03 不夠好？
A：規則碼應精準對應到 segment 與事件，不應整條路共用同一串規則。GPS-R01 是速度、GPS-R02 是貼地高速、GPS-R03 是連續高速路廊；未達連續距離門檻時應標為 partial/review，而不是整段都高危。

### Q15：是否需要 Help 與閥值設定？
A：需要。規則 Help 應說明每個 GPS-R 規則的中文名稱、判定理由、預設閥值與可調參數。閥值例如：連續路廊 20km、路廊距離 900m/1200m、高度 90m、速度 72km/h/150km/h、HDOP 3.5、衛星數 5。

### Q16：是否需要篩選器？
A：需要。建議第一階段支援日期時間、高度 N 米以下、速度 N km/h 以上、規則碼、風險等級、HDOP、衛星數、連續路廊距離、滯留時間等篩選，讓會長能快速找到異常軌跡。

### Q17：commit message 是否要包含版本號？
A：是。使用者要求後續 commit message 一律加入版本號，方便未來管理。例如：`feat(v0.3.0): 新增連續路廊防弊判定`、`fix(v0.3.1): 修復高度時間窗 hover 對齊`、`docs(v0.3.0): 新增 GPS 防弊系統 blueprint`。此規則已納入 gpsring skill。

### Q18：是否已整理需求書？
A：已新增 `blueprint.md`，定義 v0.3.0「連續路廊與擄鴿起點防弊版」需求，包含連續 20km 路廊證據、山區攔鳥網模擬資料、高度/時間小視窗、規則 Help/閥值設定、篩選器與分期實作計畫。

