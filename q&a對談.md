# Q&A 對談紀錄

本文件用於追蹤與記錄每次對談的核心需求、Q&A 解析、已完成功能以及未來待辦事項。

## 版本：v0.1.4 (2026-05-23)

### 最新 Q&A 解析與決策
1. **Q: 外包 ESP32-C3 + GPS/4G 二合一硬體與自行開發軟體有何建議？**
   * **A:** 
     * **硬體外包要點：** 
       * **極致省電與重量控制：** 鴿子負重極限為 15~20g，ESP32-C3 晶片加最小 4G 模組 (如 L76K GPS + SIM7020G 4G) 與最小鋰電池 (50~100mAh) 必須維持在 10g 以下。建議韌體支持 **Deep Sleep (深度睡眠)** 模式：在海上放飛前保持休眠，或使用加速度感測器 (IMU) 偵測到開始飛行才喚醒 GPS 與 4G。
       * **天線阻抗匹配：** 4G 與 GPS 二合一天線在極小空間內易互相干擾，外包天線調校 (RF Tuning) 與主機板防水（海上放飛防潑水與防鹽霧）是外包合約的驗收關鍵。
       * **程式燒錄與傳輸：** Type-C 必須支持 USB-to-UART 燒錄與直連讀取，並建議韌體具備標準的 USB Mass Storage 模式，插上電腦後會被識別為隨身碟，能直接讀寫、拖放與匯出加密的軌跡 CSV。
     * **軟體與韌體 Client 安裝整合：**
       * 建議使用 **ESPHome / ESP-IDF** 作為 Client 開發框架，提供極佳的 OTA (Over-the-Air) 空中升級與低功耗調度。
       * **鴿返後讀出 vs 即時 4G 上傳：** 
         * *模式一 (離線紀錄)：* 沿途僅記錄至 Flash，回巢後透過 USB / WiFi / 藍牙快速批次匯出（保證 8 小時續航）。
         * *模式二 (即時 4G)：* 每隔 n 秒記錄，每隔 m 分鐘批次打包上傳 4G，兼顧續航與即時性。

2. **Q: 點位 mouseon / hover 時應該要能顯示什麼資料？**
   * **A:** 每個點滑鼠移入時，必須顯示詳細屬性：**定位點序號 (seq)**、**台北標準時間**、**即時時速 (speed_kmh)**、**高度 (alt)**、**衛星顆數 (satellites)**、**電量 (battery_mv)** 與 **RSSI 訊號強度**，並且標註狀態（🔴 作弊高危、🟠 疑似滯留、🟢 正常飛行），提供最高水準的資料透明度。

3. **Q: 如何不透過複雜的分析，直接在畫面上看出作弊嫌疑軌跡？**
   * **A:** 
     * **公路/鐵路底圖對齊：** 地圖上新增了圖層切換控制器，使用者可自由在「黑金天網底圖 (CartoDB Dark)」、「標準公路鐵路圖 (OSM)」與「高清衛星混合圖 (Esri World Imagery)」之間切換。當切換到公路鐵路圖時，軌跡是否貼合國道、省道或高鐵一目了然。
     * **放飛關卡起點與氣象海象圖層：** 地圖上新增了 8 個北海官方放飛起點⚓圖層，並於 Popup 中載入當日天氣海象（如風向、浪高、能見度），讓風向與賽鴿軌跡的一致性一覽無遺，畫面展現極致專業。

4. **Q: 關於放飛時亂數即時媒合防弊模式（ps1）的運作與建議？**
   * **A:**
     * **模式設計：** 每一關卡上車時，GPS 鴿環資料原則預設全部清空。配對時，採用「雙環立即感應」亂數媒合：
       1. 鴿子進籠/上車前，先感應原來的 RFID 第一電子腳環。
       2. 緊接著感應 GPS 鴿環，後台立即將這兩者在資料庫中媒合綁定。
     * **防弊優勢：** 
       * **無法事先預謀：** 鴿友無法事先得知哪隻鴿子會配對到哪一個特定 GPS 鴿環，徹底斬斷「在特定 GPS 鴿環寫死模擬軌跡」的作弊空間。
       * **時空零死角：** 從感應綁定到裝籠、放飛，全程處於大會嚴密監控與封閉運輸車內，幾乎無作弊時間或空間。
     * **建議：** 
       * 後台資料庫需為此模式設計「配對交易日誌」，每次綁定時自動生成包含時間戳與操作員簽章的雜湊值 (Hash)，防止資料庫被內部人員篡改。
       * GPS 鴿環上必須安裝 NFC 晶片或雷雕二維碼，以便與 RFID 鴿鐘踏板或手持感應槍進行極速雙重感應配對。

---

## 歷史 Q&A 解析與決策 (v0.1.0)
1. **Q: 為什麼上傳 CSV 會出現 `<!DOCTYPE...` 的錯誤？**
   * **A:** 因為前端地圖（外網 `https://gps.xdove.win`）直接向內網 `192.168.120.218:8801` 呼叫 API，受到外網混合內容阻擋、跨域（CORS）或防火牆限制，導致 OpenResty 回傳了 HTML 404/501 錯誤網頁而非 JSON 數據。下一階段我們將 API 改為相對路徑並配置 Nginx / OpenResty 反向代理來解決。
2. **Q: 於 Excel 中如何看出 `timestamp` 的真實日期時間，公式如何？**
   * **A:** 使用公式 `=(((B2 + 28800) / 86400) + 25569)`（假設時間戳在 B2），並將儲存格格式設定為「日期」或自訂的 `yyyy/mm/dd hh:mm:ss`，即可還原為台灣時間 (UTC+8)。
3. **Q: 是否能在 CSV 中加入 ID 選項，以便一次性模擬/上傳多個軌跡？**
   * **A:** 可以。我們已規劃在 CSV 的第一欄加入 `id` 或 `ring_id` 欄位。上傳時，後端將自動依 ID 進行分組（Group By）並繪製多條彩色軌跡，支援一鍵批量模擬。
4. **Q: 如何對齊/提醒有問題的 ID？是否能套用物理行為規則？**
   * **A:** 已建立基於物理規則的篩選機制，包括：貼地高速飛行（坐車）、超速飛行（高鐵/超限）、異常定點停留、以及與高速公路幾何對齊。系統會自動在有問題的 ID 旁加入警示符號並提供篩選器。
5. **Q: 局部異常軌跡是否可以用橘色或紅色來代表？**
   * **A:** 可以。地圖將支援「分段渲染」（Segmented Rendering）技術：正常段為🟢綠色，局部疑似段為🟠橘色，高機率作弊段為🔴紅色，並提供異常原因浮動視窗。
6. **Q: 晶片硬體與鴿鐘的未來擴充規劃？**
   * **A:** 第一階段統一使用相容性最好的標準 **ISO 14443A (NTAG)**，鴿友可以人工用手機感應鴿環上傳。未來系統底層保留擴充欄位 `rfid_type`，以便將來升級成 **ISO 15693 (I-CODE)** 進巢踏板自動電子鴿鐘時免改寫架構。

---

### 已完成工作
* 創建並推送全新的 GitHub 儲存庫：`https://github.com/hi89817245/gpsring.git`。
* 撰寫 `物理防弊演算法.md` 規範並納入版本控制。
* **雙港合一與快取排除大修正 (v0.1.3)**：合併託管 Web 與 API，強制瀏覽器不快取 index.html，徹底解決反向代理 Port 脫節。
* **陸海氣象大升級與點位屬性 hover 自證 (v0.1.4)**：
  * **多底圖切換 (OSM 公路/鐵路、黑金、Esri 高清衛星)**，幫助肉眼直接比對軌跡是否貼合國道與高鐵。
  * **8 個北海官方放飛點關卡圖層⚓**，帶有 Popup 與當日海象氣象資訊，大幅提升專業感。
  * **定位點 hover 提示與變色渲染**，移入每個定位點可立即看見台北時間、時速、高度、衛星顆數、電量與異常狀態。
  * **CSV 下載範本大優化**：新增「高鐵極速作弊（高達 295 km/h）」、「中繼舍長時滯留」與「高速公路作弊」4 大精確嫌疑數據，並修正原本 CSV 下載時可能產生的 Nginx 404/html 亂碼。
  * **更新維護歷史 Q&A 對談紀錄**，並同步至版本控制。
