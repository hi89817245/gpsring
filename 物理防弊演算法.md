# GPS 鴿環軌跡物理防弊演算法規範

Version: v0.3.0
Last Updated: 2026-05-24

本文件定義賽鴿 GPS 軌跡的 Rule-based（基於物理規則）防弊判定邏輯。核心原則是：**不要用單點誤判作弊，而是用連續區段、時間、速度、高度、GPS 品質與場景脈絡組合判定。**

---

## 1. 預設閥值（Thresholds）

所有閥值都應能在後台/前端 Rule Profile 調整；以下為預設值。

| 參數 | 預設 | 用途 |
|---|---:|---|
| `min_corridor_match_km` | 20 km | 高速/高鐵需連續貼近至少 20km 才升高危 |
| `highway_corridor_distance_m` | 900 m | 高速/快速道路路廊容許距離 |
| `hsr_corridor_distance_m` | 1200 m | 高鐵路廊容許距離 |
| `ground_altitude_max_m` | 90 m | 貼地高速高度上限 |
| `highway_speed_min_kmh` | 72 km/h | 車輛速度下限 |
| `hsr_speed_min_kmh` | 150 km/h | 高鐵/超高速下限 |
| `normal_max_speed_kmh` | 95 km/h | 正常賽鴿高速度觀察線 |
| `gps_hdop_max` | 3.5 | GPS 品質異常 HDOP 上限 |
| `gps_satellites_min` | 5 | GPS 品質異常衛星數下限 |
| `poaching_stationary_minutes` | 30 min | 擄鴿/攔鳥網疑慮：未歸返前長時間幾乎不動 |
| `ab_stationary_minutes` | 10 min | A/B 舍或接應點：短暫停留後轉運 |
| `stationary_speed_max_kmh` | 2 km/h | 幾乎不動的速度門檻 |
| `stationary_radius_m` | 30 m | 幾乎不動的座標半徑 |

---

## 2. 分析單位：點、區段、連續 run、事件

### 2.1 點（Point）
單一 GPS 點只提供狀態線索，例如高度、速度、HDOP、衛星數。**單點不可直接判定高速/高鐵作弊。**

### 2.2 區段（Segment）
相鄰兩點構成一個 segment，計算：
- 時間差
- 直線距離
- 推算速度
- 高度變化
- 與高速/高鐵路廊距離
- GPS 品質

### 2.3 連續 run
連續多個符合相同條件的 segment 合併為 run。高速/高鐵作弊必須以 run 為證據：

```text
短段貼近路廊 < 20km  => GPS-R03/R04-PARTIAL，人工複核
連續路廊 >= 20km     => GPS-R03/R04-CORRIDOR-RUN，高危作弊
```

### 2.4 事件（Event）
將 run 或滯留行為轉成可行動事件，例如：
- 疑似攔鳥網/擄鴿起點
- A/B 舍或接應點
- 轉運起點
- GPS 品質異常區段

---

## 3. 規則碼（GPS-R Rules）

| 規則碼 | 中文名稱 | 判定邏輯 | 風險 |
|---|---|---|---|
| GPS-R01 | 超物理速度 | 速度明顯高於賽鴿合理上限，但需排除 GPS 品質異常 | Warning/Critical |
| GPS-R02 | 貼地高速 | 低高度 + 車輛速度 | Warning |
| GPS-R03-PARTIAL | 高速/快速道路短段貼近 | 符合貼地高速與高速路廊距離，但未達連續 20km | Review |
| GPS-R03-CORRIDOR-RUN | 高速/快速道路連續路廊 | 連續貼近高速/快速道路達 `min_corridor_match_km` | Critical |
| GPS-R04-PARTIAL | 高鐵短段貼近 | 高速且靠近高鐵，但未達連續 20km | Review |
| GPS-R04-CORRIDOR-RUN | 高鐵連續路廊 | 連續貼近高鐵達 `min_corridor_match_km` | Critical |
| GPS-R05 | 非登記點滯留 | 非終點/非合法檢查點，連續低速或幾乎不動 | Warning |
| GPS-R06 | GPS 品質異常 | HDOP 高、衛星數低、漂移跳點 | Device Review |
| GPS-R07 | 低頻不可判定 | 採樣間隔太大，無法可靠判斷短段事件 | Unknown/Review |
| GPS-R08 | 疑似擄鴿/攔鳥網起點 | 未歸返前，山區/非登記點長時間幾乎不動，後續出現轉運 | Warning/Critical |
| GPS-R09 | 山區異常滯留 | 山區、稜線、谷口等非正常歸返點停留 | Warning |
| GPS-R10 | 轉運起點 | 滯留後接續公路/高鐵/快速移動 run | Critical Context |

---

## 4. 異常滯留類型分流

使用者特別指出：異常滯留不應只有一種，至少分為「擄鴿」與「A/B 舍」。

### A. 擄鴿 / 攔鳥網

**情境：** 鴿子尚未歸返前，GPS 座標幾乎不動，但仍持續回報訊號。

判定條件：
1. 尚未到達登記鴿舍或合法檢查點。
2. 連續訊號存在。
3. 位置半徑小於 `stationary_radius_m`。
4. 持續時間 >= `poaching_stationary_minutes`，預設 30 分鐘。
5. 地點位於山區、稜線、谷口、偏僻路徑、或歷史熱點。
6. 後續若接上車輛/高鐵路廊，升級為 GPS-R08 + GPS-R10。

UI 表示：
- 使用 🕸️ 或紅紫色特殊圖示。
- 事件列表顯示「疑似攔鳥網/擄鴿起點」。
- 4G 版本可即時通報會長或管理員。

### B. A/B 舍 / 接應點

**情境：** 不會長時間停留，通常停留不超過 10 分鐘，接著開車走公路/快速道路/高速公路，或取下 GPS 鴿環換鴿。

判定條件：
1. 非登記鴿舍/非合法檢查點。
2. 滯留時間介於數分鐘到 `ab_stationary_minutes`，預設 10 分鐘。
3. 後續速度突然轉為車輛速度。
4. 後續軌跡貼近公路/快速道路/高鐵路廊。
5. 若 GPS 高度偏低且速度穩定，升級為 GPS-R02 + GPS-R03/R04。

UI 表示：
- 使用 🏚️ 或 🚗 圖示。
- 顯示「疑似 A/B 舍或接應轉運」。
- 若連續路廊達 20km，事件升級為 Critical。

---

## 5. 高速/高鐵連續路廊演算法

### 5.1 為何不能只看幾點？

幾個點靠近高速公路或高鐵，可能只是：
- 放飛或歸返路徑自然交會
- 地圖路廊誤差
- GPS 漂移
- 低頻抽樣造成直線跨越

因此只要未達連續距離門檻，就應列為 partial/review。

### 5.2 演算法

1. 對每個 segment 計算與高速/高鐵路廊的最短距離。
2. 同時檢查速度與高度。
3. 將連續符合條件的 segment 合併成 run。
4. 累積 run distance。
5. 若 distance >= `min_corridor_match_km`，才升級高危。

```pseudo
for segment in track:
  if speed >= highway_speed_min and alt <= ground_altitude_max and distance_to_highway <= highway_corridor_distance:
    segment.type = highway_corridor_candidate
  elif speed >= hsr_speed_min and alt <= ground_altitude_max and distance_to_hsr <= hsr_corridor_distance:
    segment.type = hsr_corridor_candidate
  else:
    segment.type = normal_or_other

runs = group_consecutive_segments_by_type(segment.type)
for run in runs:
  if run.distance_km >= min_corridor_match_km:
    mark_critical(run)
  else:
    mark_partial_review(run)
```

---

## 6. 高度 / 時間維度小視窗

平面地圖不足以判讀作弊，應新增 timeline panel（時間線面板）：

- X 軸：時間。
- Y 軸可切換：高度、速度、HDOP、衛星數。
- hover 地圖點時 timeline 高亮。
- hover timeline 時地圖 marker 高亮。
- 點擊 GPS-R08/GPS-R10 事件時，自動跳到該時間點。

用途：
- 看出是否突然降高度、貼地高速。
- 看出是否在山區長時間不動。
- 看出 GPS 品質異常與作弊行為的差異。

---

## 7. 篩選功能

第一階段建議提供：

| 篩選 | 用途 |
|---|---|
| 日期時間 | 只看某一段賽程 |
| 高度 N 米以下 | 快速找貼地高速或滯留 |
| 速度 N km/h 以上 | 找車輛/高鐵嫌疑 |
| 規則碼 | 只看 GPS-R03/GPS-R08 等事件 |
| 風險等級 | 只看 Critical/Warning |
| HDOP/衛星數 | 分離 GPS 故障與作弊 |
| 連續路廊距離 | 只看 >=20km 的高危 run |
| 滯留時間 | 找 10 分鐘 A/B 舍或 30 分鐘擄鴿 |

---

## 8. 視覺化渲染規範

| 顏色/圖示 | 意義 |
|---|---|
| 🟢 藍綠線 | 正常飛行 |
| 🟠 橘線 | 疑似異常、滯留、短段路廊待複核 |
| 🔴 紅色虛線 | 高危作弊，連續路廊或交通工具證據成立 |
| 🟣 紫線 | GPS 品質/設備定位疑慮 |
| 🕸️ | 疑似攔鳥網/擄鴿起點 |
| 🏚️ | 疑似 A/B 舍或中繼舍 |
| 🚗 | 車輛轉運起點 |
| 🚄 | 高鐵轉運起點 |

Mouseover（滑鼠懸停）應顯示中文：
- 設備 ID
- 點號 / 區段號
- 台北時間
- 速度
- 高度
- 規則碼
- 中文判定原因
- 連續路廊距離 / 滯留時間（若有）

---

## 9. 驗收資料集

至少應有以下 demo CSV：

1. `template_normal.csv`：正常歸返。
2. `template_cheat.csv`：連續高速/快速道路轉運，需看得出 >=20km 路廊。
3. `template_hsr.csv`：連續高鐵路廊。
4. `template_suspicious.csv`：A/B 舍或接應點短暫停留後轉運。
5. `template_gps_fault.csv`：GPS 品質異常，不可直接判作弊。
6. `template_poaching_mountain_net.csv`：山區攔鳥網/擄鴿，滯留後轉運。

---

## 10. 最小驗收條件

- 高速/高鐵少數點貼近時，不可直接紅線高危。
- 預設連續 20km 路廊才可判高危，且 20km 必須是可調閥值。
- 擄鴿與 A/B 舍需能分流，不可都叫「異常滯留」。
- GPS 品質異常需標成設備/定位疑慮，不可混成作弊。
- 地圖線條與點位 mouseover 必須中文化。
- 文件、blueprint、演算法與韌體狀態機必須同步更新。
