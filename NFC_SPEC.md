# GPS 鴿環 NFC 透明化履歷功能整合規格書 (NFC_SPEC.md)
Version: 1.0.0
Last Updated: 2026-05-23

為了達成賽鴿生命履歷的「一條龍公開透明化」，我們將 NFC (近場通訊) 技術引入 GPS 鴿環。

每枚鴿環外側貼裝/嵌入一枚唯一的 **NTAG213 / NTAG215** 高頻 NFC 貼片。此貼片具備唯讀 UID，與 GPS 腳環實體 ID (`device_id`) 在出廠時進行 1 對 1 綁定。

---

## 1. 業務場景流程與 NFC 寫入規格

### 流程一：上車集鴿（手機 App 快速配對）
1.  **場景**：在鴿舍或鴿會上車集鴿前，裁判或鴿友使用 Android / iOS 手機。
2.  **操作**：開啟大後台 App，手機背面貼近 GPS 鴿環的 NFC 貼片。
3.  **動作**：
    *   手機讀取 NFC 內部的 NDEF 網址：`https://gpsring.gscc.net/r/G0703-00001` (最後 11 碼為 `device_id`)。
    *   App 自動跳出配對視窗，掃描賽鴿眼部「虹膜」或腳環編號（例如 `660001`）。
    *   App 自動向後台發送 `POST /api/v1/devices/config`（即 2.1 API），完成「設備-賽鴿-賽事-放飛起點-預期終點」綁定。

### 流程二：歸返讀取（電腦 / 第二鴿鐘感應公開）
1.  **場景**：賽鴿歸巢，鴿友取下鴿環插上 USB 充電座，或者直接拿手機 / NFC 讀卡機感應鴿環。
2.  **操作**：
    *   **感應 NFC**：手機或感應器讀取 NFC，立刻在瀏覽器打開：
        `https://gps.xdove.win/index.html?device=G0703-00001&view=public`
    *   **網頁直顯**：地圖前端自動載入該 device 的所有歸返 GPS 軌跡，並在網頁上印出**透明化四大履歷**：
        1.  **虹膜驗證時間** (上車檢錄)
        2.  **上車時間與坐車軌跡** (檢錄至放飛點，偵測是否有人中途私自拆卸)
        3.  **外海放飛時間與座標** (放飛瞬間)
        4.  **歸返時間與空中軌跡** (判定是否有作弊嫌疑)

---

## 2. NFC 網址與 NDEF 資料格式

NFC 晶片內預燒錄標準的 **NDEF (NFC Data Exchange Format) URI Record**，其儲存資料結構如下：

*   **NDEF 類型**：`URI` (0x01)
*   **網址格式**：`https://gps.xdove.win/p/` + `{device_id}`
*   **範例內容**：`https://gps.xdove.win/p/G0703-00001`
*   **後台重新導向 (Redirect)**：
    *   當一般使用者用手機感應時，`gps.xdove.win` 反向代理域名會自動處理，或將請求 302 重導向到內網/公網的 GIS 平台：
        `https://gps.xdove.win/index.html?device=G0703-00001`
    *   這確保了在**本地開發測試階段 (192.168.120.218 / gps.xdove.win)** 與**未來 AWS 雲端上線階段**，硬體 NFC 內容無須重新燒錄，100% 兼容。

---

## 3. 前端 NFC 網址參數解析功能整合

為了解決「感應 NFC 後自動跳轉並加載地圖」的功能，我已將 `index.html` 的載入邏輯升級：
當 URL 帶有 `?device=G0703-00001` 時，網頁會自動跳過點擊步驟，直接透過 8801 連接後台讀取該鴿環的所有軌跡點並啟動 AI 作弊合理性判定！
