# GPS 鴿環專案驗收指標與進度規劃 (驗收點.md)
Version: 1.0.0
Last Updated: 2026-05-20

本文件為 **我方 (軟體與自行組裝/外殼整合開發)** 與 **硬體外包商 (硬體設計/雙合一天線/低功耗 MCU 韌體 BSP)** 的雙向驗收與時程進度合規標準。

---

## 1. 專案開發階段規劃

```
【Phase 1: POC 概念驗證】 ───► 【Phase 2: EVT 工程樣機】 ───► 【Phase 3: DVT 設計驗證】 ───► 【Phase 4: PVT 量產測試】
 3-4 週 (現階段測試)            4-5 週 (硬體打樣/假外殼)        4 週 (防水/摔落/PCB定型)       2-3 週 (少量生產/實測)
```

---

## 2. 雙方驗收對接清單 (雙向檢驗標準)

### 2.1 外包硬體商 ➔ 我方 (硬體與底層韌體交付驗收)
外包商應於各個階段（POC、EVT、DVT）依序交付以下可量化的指標實物與數據報告：

| 項目分類 | 驗收功能指標 | 測試與驗收標準 | 狀態/備忘 |
| :--- | :--- | :--- | :--- |
| **外觀與重量** | **1. 裸機重量控制** | PCB 裸板 + 最小鋰電池(302030, 130mAh,約2.6g) + BDS/GPS微型定位模組(GP-02, 約0.2g) + 2.4G/GPS二合一天線 + ESP32-C6 (省電核心) 總重量必須控制在 **< 6g**，為未來預留 4G 模組 (約 1.5g) 的配重與空間。 | ⚠️ 重量上限由 10g 下調至 **6g** (暫不貼裝4G) |
| **續航與功耗** | **2. 續航力達成** | 在預設 Wi-Fi 關閉、GPRS 離線儲存（10秒定位記錄一次）的 `offline/hybrid` 模式下，實測使用 130mAh 電池必須持續運作 **8 至 10 小時**。 | ⚠️ 原 8 小時改為 **8-10 小時** 保險安全餘量 |
| | **3. 低功耗休眠** | 支援由 ESP32-C3 深度休眠 (Deep Sleep) 或 GPS 週期喚醒 (`low_power`)，休眠電流需達微安級 ($\le 50 \mu A$)。 | 配合設定中的 `gps_power_mode` |
| **Wi-Fi 預設** | **4. 預設測試熱點** | 韌體出廠前需預設寫入測試 SSID：**`gscc3`**，密碼：**`2965084522053`**。當設備開機偵測到此熱點時，自動上傳未上報之軌跡點。 | ⚠️ 新增預設，方便鴿舍統一測試 |
| **通訊與儲存** | **5. Flash 儲存安全** | 當無 4G/Wi-Fi 網路訊號時，必須將資料快取於晶片 Flash。需能安全快取至少 **12 小時 (約 4,320 點)** 的 GPS 記錄，且斷電不遺失。 | 離線安全防護 |
| | **6. eSIM 通訊預留** | EVT 階段需使用 Wi-Fi/GP-02 進行位置記錄，PCB Layout 必須預留 4G Cat-1 模組 (LCC/LGA 微型封裝如 WH-GM800) 的空貼焊盤與 eSIM 天線匹配路徑。 | ⚠️ 4G 模組改為「預留不貼裝」設計，避開台灣4G頻段早期限制與初期超重問題 |
| **低層 API** | **7. 底層 API/SDK 交付**| 交付符合 C/C++ (ESP-IDF/Arduino + ESPConnect) 的應用層 Client，支援預留 OTA 連接點。 | C6 省電核開發源碼與 BSP |
| **Wi-Fi 設定** | **8. 遠端 Wi-Fi 設定下發**| 支援透過 API 遠端發送 Wi-Fi 的 SSID 與 密碼 設定至裝置 (ConfigPayload)。 | ⚠️ 新增 Wi-Fi 欄位，支援設備熱點動態變更 |

---

### 2.2 我方 ➔ 外包硬體商 (自建 GIS 後台與調測環境驗收)
我方負責之軟體、分析與測試平台需在 EVT/DVT 之前完成，用以支持與檢驗硬體端之上傳：

| 項目分類 | 驗收功能指標 | 測試與驗收標準 | 當前進度 |
| :--- | :--- | :--- | :--- |
| **接口通訊** | **1. 設定 API (`:8801`)** | 提供 `POST /api/v1/devices/config` 設定接口。 |  已實作 (Port 8801) |
| | **2. 數據上報 API** | 提供 `POST /api/v1/tracks/ingest` 高頻軌跡批次接收接口。 |  已實作 (Port 8801) |
| **GIS 視覺化** | **3. GIS 地圖軌跡線** | 前端網頁 (`:8802`) 需可載入設備對應的 GPS 軌跡，並即時在地圖繪製歷史航線。 |  已實作 (Port 8802) |
| **防弊分析** | **4. 坐車作弊檢測** | 系統需自動分析，若有與高速公路貼合、速度不合理、中繼舍停留等，需亮紅燈並給予 Risk Score。 |  已實作 (內置 3 大演算法) |
| **實體整合** | **5. 防水防塵外殼** | 我方負責外殼 3D 建模與防水設計（目標 **IP67** 磁吸充電接口，無 USB 物理開孔）。 | 設計中 (配合 Pogo Pin) |

---

## 3. 開發排程與里程碑 (Milestones)

### 第一階段 (當前): 軟體就緒與模擬聯調 (當前 1-2 週)
- **硬體商任務**: 完成 ESP32-C3 + GPS 的 POC 開發板打樣，驗證在預設熱點 `gscc3` 下能獲取經緯度並發送 HTTP Request。
- **我方任務**: 提供 `:8801` (API Ingestion) 與 `:8802` (GIS 前端) 的運作環境（在 192.168.120.218 或本機上部署）。

### 第二階段: EVT 工程樣機與物理配重測試 (4-5 週)
- **硬體商任務**:
  1. 將虛擬 eSIM、二合一天線、鋰電池與主板整合並微型化。
  2. 交付 5-10 組工程裸板樣機。
  3. 實測在 10s 採樣、Wi-Fi 停用下，電池能維持 **8 到 10 小時** 連續工作。
- **我方任務**: 完成 IP67 外殼打樣、上機配重（控制總重量小於 20g 夾扣於賽鴿腳上），並與硬體商實施 Wi-Fi 聯調測試。

### 第三階段: DVT 設計驗證與實際拉線試飛 (4 週)
- **硬體商任務**: PCB 定型與量產前電性能、天線收訊、低功耗優化，修訂底層 BSP。
- **我方任務**: 與鴿友合作進行實際賽鴿帶環「試飛測試」，並將收集到的真實軌跡匯入 GIS 系統，校正「作弊軌跡分析引擎」的敏感度。
